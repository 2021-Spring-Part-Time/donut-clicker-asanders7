// export const donutStats = (donutInfo) => {
//     return (
//     ' Donut Count: ' +
//     donutInfo.donutCount +
//     ' Auto Clicker Count: ' +
//     donutInfo.autoClickerCount +
//     ' Auto Clicker Cost: ' +
//     donutInfo.autoClickerCost +
//     ' Donut Multiplier Count: ' +
//     donutInfo.donutMultiplierCount +
//     ' Donut Multiplier Cost: ' +
//     donutInfo.donutMultiplierCost
//     );
// };

export const donutCount = (donutInfo) => {
    return ( ' Donut Count: ' +
    donutInfo.donutCount);
};

export const autoClickerCount = (donutInfo) => {
    return ( ' Auto Clicker Count: ' +
    donutInfo.autoClickerCount);
};

export const autoClickerCost = (donutInfo) => {
    return ( ' Auto Clicker Cost: ' +
    donutInfo.autoClickerCost);
};

export const donutMultiplierCount = (donutInfo) => {
    return ( ' Donut Multiplier Count: ' +
    donutInfo.donutMultiplierCount);
};

export const donutMultiplierCost = (donutInfo) => {
    return ( ' Donut Multiplier Cost: ' +
    donutInfo.donutMultiplierCost);
};

export const donutAmtPerClick = (donutInfo) => {
    return ( ' Donut Amount Per Click: ' +
    donutInfo.donutAmtPerClick);
};